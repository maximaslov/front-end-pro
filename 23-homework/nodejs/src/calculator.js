const add = require('./add.js').add;
const sub = require('./sub.js').sub;
const mult = require('./mult.js').mult;
const div = require('./div.js').div;

module.exports = {
    add,
    sub,
    mult,
    div,
}